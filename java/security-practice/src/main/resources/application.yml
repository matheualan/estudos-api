server:
  port: 8080

spring:
  application:
    name: security-practice

  profiles:
    active: prod

  jpa:
    open-in-view: false
    properties:
      hibernate:
        format_sql: true

logging:
  level:
    root: INFO
    org.springframework.web: INFO
    com.myapp: DEBUG