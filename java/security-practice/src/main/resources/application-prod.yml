server:
  port: 8081

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/security-practice?createDatabaseIfNotExist=true&useSSL=true&allowPublicKeyRetrieval=true&serverTimezone=UTC
    driver-class-name: com.mysql.cj.jdbc.Driver
#    username: root
#    password: root
    username: ${DB_USERNAME_MYSQL}
    password: ${DB_PASSWORD_MYSQL}

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    database-platform: org.hibernate.dialect.MySQL8Dialect

  flyway:
    enabled: false
    baseline-on-migrate: true
    locations: classpath:db/migration
    url: jdbc:mysql://localhost:3306/security-practice?useSSL=false&serverTimezone=UTC
    user: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver

#logging:
#  level:
#    root: INFO
#    org.springframework.web: WARN
#    com.security: INFO
#
#management:
#  endpoints:
#    web:
#      exposure:
#        include: health,info
#  endpoint:
#    health:
#      show-details: never